import React from "react";
import { Card } from "antd";
import "./StoryList.scss"

function StoryList({style}){
    return (
        <div style={style}>
            <Card title="stories" size="small">StoryList ...</Card>
        </div>
    )
};

export default StoryList;